// MongoDB module stubbed out as Supabase PostgreSQL is the exclusive database
const connectDB = async () => {
  // SQLite and MongoDB are completely removed.
  // Supabase PostgreSQL handles database persistence.
  return Promise.resolve();
};

export default connectDB;
